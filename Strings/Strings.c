#include <stdio.h>
#include <stdlib.h>

int main(){

    //exemplo Strings
       char str [40];
      printf ("\nDigite a String desejada= ");
      gets (str);
      printf("Sua string e: ");
      puts(str);



    return 0;
}